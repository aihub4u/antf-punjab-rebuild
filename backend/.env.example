# Copy this file to .env and fill in real values. Never commit .env itself.

# Database (same SQL Server DB the current .NET app uses)
DB_SERVER=hitesh_police.mssql.somee.com
DB_NAME=hitesh_police
DB_USER=rbhartiya_SQLLogin_3
DB_PASSWORD=
DB_PORT=1433

# Auth
JWT_SECRET=change-this-to-a-long-random-string
PORT=4000
FRONTEND_URL=http://localhost:5173

# Mobile/WhatsApp inbound webhook signature validation (MobileListener)
# These were previously hardcoded byte arrays directly in MobileListener.aspx.cs.
# Get the real values from whoever manages the mobile app / Karix integration -
# do NOT generate new ones, the caller encrypts with a key that must match.
# Format: JSON array of bytes, e.g. [56,95,43,64,75,96,26,35,64,73,82,98,31,47,68,72]
MOBILE_AUTH_KEY=
MOBILE_AUTH_IV=

# Outbound WhatsApp notification gateway (B2BWebService replacement)
# Previously hardcoded in B2BWebService.aspx.cs.
WHATSAPP_GATEWAY_URL=https://rcmapi.instaalerts.zone/services/rcm/sendMessage
WHATSAPP_GATEWAY_TOKEN=
WHATSAPP_SENDER_NUMBER=919779100200
